import { Camera } from "../camera/camera";
import { Scene } from "../scene/scene";

export class WebGLRenderer {
  render(scene: Scene, camera: Camera) {
    // get all objects.
    // for each objectw, bind buffer and drawArray
  }
}
